# Store

Shared client state belongs here when local component state is no longer enough.

Prefer React state and server data fetching first. Add a store only for cross-page UI state or cached client state that is truly shared.
