# Features

Feature-level UI, hooks, and local helpers live here when a module grows beyond a single route page.

Keep Next.js route files in `app/`, then move reusable module logic here by domain, for example:

- `features/products`
- `features/users`
- `features/transactions`
- `features/tickets`
